{
  "name": "copilot-agent-portfolio-site",
  "version": "2.0.0",
  "description": "Ryan Gildea portfolio — audio engineering, mixing, and production.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "serve": "astro preview --port 8080",
    "test": "playwright test",
    "test:visual": "PW_QUICK=1 playwright test",
    "test:visual:full": "playwright test",
    "test:visual:quick": "PW_QUICK=1 playwright test",
    "test:visual:debug": "PW_QUICK=1 playwright test --headed --workers=1 --reporter=line"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "isomorphic-dompurify": "^3.11.0",
    "marked": "^13.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1"
  }
}
